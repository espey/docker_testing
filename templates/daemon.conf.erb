[Service]
ExecStart=
ExecStart=/usr/bin/docker daemon -H fd:// -H tcp://0.0.0.0:2375 --storage-driver devicemapper --storage-opt dm.fs=xfs --storage-opt dm.thinpooldev=/dev/mapper/<% @vol_group %>-<% @logical_vol %> --storage-opt dm.use_deferred_removal=true --storage-opt dm.use_deferred_deletion=true
